'use client';

import React from 'react';
import { AuthLayout, Login } from '@mas-salud/components/templates';

const LoginPage: React.FC = () => {
  return (
    <AuthLayout>
      <Login />
    </AuthLayout>
  );
};

export default LoginPage;
